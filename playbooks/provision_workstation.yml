---
- name: Provision Workstations
  hosts: workstations
  become: false
  tags: workstation
  roles:
    - role: users
      when: not (skip_user_creation | default(false) | bool)
    - common
    - toolchains
    - ai_tools
    - terminal
    - editor
    - docker
    - system
    - gui_apps
