---
- name: Install Common Core Packages (macOS)
  community.general.homebrew:
    name: "{{ core_packages }}"
    state: present
    update_homebrew: yes

- name: Start Syncthing service (macOS)
  community.general.homebrew_services:
    name: syncthing
    state: present

